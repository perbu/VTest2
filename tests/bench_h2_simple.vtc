vtest "HTTP/2 Simple Request Benchmark"

# Simple single stream request/response for benchmarking

server s1 {
    rxreq
    txresp -status 200 -body "OK"
} -start

client c1 -connect ${s1_sock} {
    txreq -url "/"
    rxresp
    expect resp.status == 200
} -run
